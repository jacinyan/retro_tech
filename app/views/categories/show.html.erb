<%= link_to("<< Back to List", categories_path, :class => 'back-link') %>

<div class="subjects show">
  <h2>Show Category</h2>

  <table summary="Subject detail view">
    <tr>
      <th>Name</th>
      <td><%= @category.name %></td>
    </tr>
    <tr>
      <th>Created</th>
      <td><%= @category.created_at.to_s(:long) %></td>
    </tr>
    <tr>
      <th>Updated</th>
      <td><%= @category.updated_at.strftime("%m/%d/%Y %I:%M %p") %></td>
    </tr>
  </table>

</div>
